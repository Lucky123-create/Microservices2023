---
server:
  port: 9198
  servlet:
    context-path: /owners
message: Hello from github
spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
    hikari:
      maximum-pool-size:  2


